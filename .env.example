# Copy to .env.local and fill in from Supabase → Project Settings → API.
# Leave empty to run LifeOS in local demo mode (data stays in this browser).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
